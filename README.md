# dmzapp

## Disclaimer
This is a deliberately vulnerable app for training purposes. You should not use this app for any reason whatsoever. If you DO decide to run this app, the risk is on you :)

## Setup and Installation
1. clone the repo
2. run `init.sh` to set up the SSL self-signed certs and the ssh keys
3. `docker compose up --build -d`
